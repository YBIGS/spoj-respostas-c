#include <iostream>
using namespace std;

int main()
{
    int m = 0;
    cin >> m;

    if(m <= 100 && m >= 0)
    {
        if (m == 0)
          cout << "E";

        if (m > 0 && m <= 35)
            cout << "D";

        if (m > 35 && m <= 60)
            cout << "C";

        if (m > 60 && m <= 85)
            cout << "B";

        if (m > 85)
            cout << "A";
    }

    return 0;
}
